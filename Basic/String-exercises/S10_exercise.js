// ==================================================
// EJERCICIO 10
// Comprueba si dos strings tienen la misma longitud
// ==================================================

// Declaramos dos variables de tipo string
const string1 = "JavaScript"
const string2 = "Python"

// Comparamos la longitud de los strings usando length
string1.length === string2.length
    ? console.log("Los strings tienen la misma longitud") 
    : console.log("Los strings tienen diferente longitud")